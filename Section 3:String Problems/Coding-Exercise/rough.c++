#include <bits/stdc++.h>
using namespace std;
 
int main()
{
      string str = "0x71f";
      unsigned long num = stoi(str,nullptr,16);
      cout << "The base ten equivalent of FF is " << num << "\n";
}