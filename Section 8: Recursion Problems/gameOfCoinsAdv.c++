#include<bits/stdc++.h>
using namespace std;

tuple<int,int,int> maxVal(vector<int> vctr , int i , int j , int k)
{
    if(i > j or k <= 0)
    {
        return 0;
    }
    int a = vctr[i] + max()
}

int MaxValue(int n, vector<int> v, int k){
    //Write your code here. Do not modify the function or parameters. You can use a helper function if needed.
    
}
